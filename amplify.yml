version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "GEMINI_API_KEY=$GEMINI_API_KEY" >> .env.production`n       
         - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - node_modules/**/*
